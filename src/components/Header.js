function Header() {
  return (
    <div
      style={{
        padding: "12px 16px",
        fontSize: "20px",
        fontWeight: "bold",
        borderBottom: "1px solid #ddd"
      }}
    >
      StreamNest
    </div>
  );
}

export default Header;